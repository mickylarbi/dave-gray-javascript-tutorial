if (player === computer) {
    // tie game!
} else if (player === 'rock') {
    if (computer === 'scissors') {
        // player wins!
    } else {
        // computer wins
    }
} else if (player === 'paper') {
    if (computer === 'rock') {
        // player wins!
    } else {
        // computer wins
    }
} else if (player === 'scissors') {
    if (computer === 'paper') {
        // player wins!
    } else {
        // computer wins
    }
}